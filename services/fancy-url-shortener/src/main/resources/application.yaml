spring:
  application:
    name: fancy-url-shortener
    redirect-base-url: "http://localhost:8000/"
  cassandra:
    keyspace-name: fancy_url_shortener
    contact-points: localhost
    port: 9042
    local-datacenter: datacenter1
